class CalendarioController < ApplicationController
  def index
  	#@test = Curso.select(:profesor_id).group(:materium_id).count
  	#file = "app/assets/javascripts/pruebainscripcion3.csv"
  	#CSV.open(file, "w") do |writer|
  	#	writer << ["materia","profesores"]
  	#	@test.each do |m|
  	#		writer << [m[0],m[1]]

  	#	end
  	#end
    #response = HTTParty.get("http://localhost:3000/grafico1.json", timeout: 5)
    #puts response.to_a;
    #puts response.body;
    #puts response.to_h;
    #json = JSON.parse(response.body);

  #var rawData = JSON.parse("localhost:3000/grafico1.json");

  #for(key in rawData){
   # if(rawData.hasOwnProperty(key)){
    #    data.push({'curso' : key, 'estudiantes' : rawData[key]});
    #}
  #} 

  end
end
